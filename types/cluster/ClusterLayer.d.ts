import React from 'react';
import { ClusterLayerProps } from "./cluster.type";
export declare const ClusterLayer: ({ points, clusterOptions, options, onClick, onOver, onSourceCreated, clusterStyle, }: ClusterLayerProps) => React.JSX.Element;
