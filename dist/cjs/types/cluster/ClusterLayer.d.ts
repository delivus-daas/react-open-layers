import React from 'react';
import { ClusterLayerProps } from "./cluster.type";
export declare const ClusterLayer: ({ points, clusterOptions, options, onClick, className, onOver, onSourceCreated, clusterStyle, }: ClusterLayerProps) => React.JSX.Element;
