import { ClusterLayerProps } from "./cluster.type";
export declare const useCluster: ({ features, map, clusterOptions, layerOptions, onClick, onOver, clusterStyle: clusterStyleProp, overStyle, clickStyle, visible }: ClusterLayerProps) => void;
